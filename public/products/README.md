# Product & category imagery

Add category and product photos here using the file names in
`docs/image-generation-prompts.md` (e.g. `steaks-beef.jpg`,
`seafood-salmon.jpg`, or per-product `{slug}.jpg`).

Tasteful placeholders render until real photos land.
